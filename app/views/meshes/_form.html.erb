<%= form_for(@mesh) do |f| %>
  <% if @mesh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mesh.errors.count, "error") %> prohibited this mesh from being saved:</h2>

      <ul>
      <% @mesh.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :stl_files %><br>
    <%= f.text_field :stl_files %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
